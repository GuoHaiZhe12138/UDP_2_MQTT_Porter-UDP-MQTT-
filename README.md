# SentinelCast 🚀

SentinelCast 是一款基于 C++17 工业级数据转发网关。它能高效地将 **UDP 组播报文** 转换为 **MQTT 消息**。

### 核心特性
* **异步双线程**：UDP 接收与 MQTT 发送完全解耦，内置 `SafeQueue` 防止高频丢包。
* **线程安全**：使用 `std::atomic` 和 `std::mutex` 确保 QoS 及 Topic 配置的动态安全修改。
* **边缘优化**：支持 `if_nametoindex` 动态绑定物理网卡（如 eth0/wlan0）。
* **极简 API**：三行逻辑即可完成从初始化到启动的全过程。

---

### 📦 快速集成

```cpp
#include "SentinelGate.hpp"

int main() {
    // 1. 初始化 (MQTT Broker, Port)
    sentinel::SentinelGate gate("192.168.1.10", 1883);

    // 2. 配置 (可选)
    gate.setMqttTopic("sentinel/sensor/raw");
    gate.setMqttQos(1);

    // 3. 启动转发 (组播IP, 端口, 物理网卡名)
    gate.start("224.0.1.100", 8080, "eth0");

    while (true) { pause(); } // 保持运行
    return 0;
}
